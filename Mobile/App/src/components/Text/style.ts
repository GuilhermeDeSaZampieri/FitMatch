import { StyleSheet } from 'react-native';
import { THEME } from '../../assets/THEME'; 

export const styles = StyleSheet.create({
  body: {
    fontFamily: THEME.FONTS.DMSans.regular,
    fontSize: 16,
    lineHeight: 24,
    color: THEME.COLORS.gray,
  },

  labelInput: {
    fontFamily: THEME.FONTS.DMSans.semiBold,
    fontSize: 16,
    lineHeight: 20,
    color: THEME.COLORS.gray,
  },
  welcomeLvl:{
    fontFamily: THEME.FONTS.DMSans.regular,
    fontSize: 14,
    lineHeight: 14,
    color: THEME.COLORS.white
  },
  welcomeLower:{
    fontFamily: THEME.FONTS.DMSans.medium,
    fontSize: 15,
    lineHeight: 18,
    color: THEME.COLORS.white
  },
  welcomeTitle:{
    fontFamily: THEME.FONTS.DMSans.medium,
    fontSize: 30,
    lineHeight: 35,
    color: THEME.COLORS.white
  },
  labelCategory: {
    fontFamily: THEME.FONTS.DMSans.semiBold,
    fontSize: 16,
    lineHeight: 20,
    color: THEME.COLORS.black,
  },

  labelChoose: {
    fontFamily: THEME.FONTS.DMSans.medium,
    fontSize: 14,
    lineHeight: 14,
    color: THEME.COLORS.grayDeep, 
  },

  labelChooseFocus: {
    fontFamily: THEME.FONTS.DMSans.medium,
    fontSize: 14,
    lineHeight: 14,
    color: THEME.COLORS.black,
  },

  placeholder: {
    fontFamily: THEME.FONTS.DMSans.regular,
    fontSize: 16,
    lineHeight: 24,
    color: THEME.COLORS.grayLight,
  },

  small: {
    fontFamily: THEME.FONTS.DMSans.regular,
    fontSize: 12,
    lineHeight: 16,
    color: THEME.COLORS.gray,
  },

  desc: {
    fontFamily: THEME.FONTS.DMSans.regular,
    fontSize: 12,
    lineHeight: 16,
    color: THEME.COLORS.grayDark,
  },

  levelSmall: {
    fontFamily: THEME.FONTS.DMSans.semiBold,
    fontSize: 12,
    lineHeight: 12,
    color: THEME.COLORS.blackSub,
  },

  level: {
    fontFamily: THEME.FONTS.DMSans.bold,
    fontSize: 25,
    lineHeight: 25,
    color: THEME.COLORS.blackSub,
  },
  titleAchievements:{
    fontFamily: THEME.FONTS.DMSans.bold,
    fontSize: 18,
    lineHeight: 20,
    color: THEME.COLORS.grayDeep,
  },
  pointsNextLevel: {
    fontFamily: THEME.FONTS.DMSans.medium,
    fontSize: 10,
    lineHeight: 15,
    color: THEME.COLORS.grayDeep,
  },
  pointsCount:{
    fontFamily: THEME.FONTS.DMSans.regular,
    fontSize: 16,
    lineHeight: 26,
    color: THEME.COLORS.blackPts,
  },
  ptsPointsCount:{
    fontFamily: THEME.FONTS.DMSans.regular,
    fontSize: 12,
    lineHeight: 26,
    color: THEME.COLORS.blackPts,
  },

  medal: {
    fontFamily: THEME.FONTS.DMSans.light,
    fontSize: 12,
    lineHeight: 12,
    color: THEME.COLORS.black,
  },

  mapVisibility: {
    fontFamily: THEME.FONTS.DMSans.semiBold,
    fontSize: 16,
    lineHeight: 20,
    color: THEME.COLORS.grayDark,
  },

  title: {
    fontFamily: THEME.FONTS.Bebas.regular,
    fontSize: 32,
    lineHeight: 36,
    color: THEME.COLORS.black,
  },

  lowerTitle: {
    fontFamily: THEME.FONTS.Bebas.regular,
    fontSize: 28,
    lineHeight: 32,
    color: THEME.COLORS.black,
  },

  subtitle: {
    fontFamily: THEME.FONTS.Bebas.regular,
    fontSize: 20,
    lineHeight: 20,
    color: THEME.COLORS.blackSub,
  },

  nameParticipants: {
    fontFamily: THEME.FONTS.Bebas.regular,
    fontSize: 16,
    lineHeight: 16,
    color: THEME.COLORS.black,
  },

  seeMore: {
    fontFamily: THEME.FONTS.Bebas.regular,
    fontSize: 15,
    lineHeight: 17,
    color: THEME.COLORS.blackSub,
  },

  titleActivity: {
    fontFamily: THEME.FONTS.DMSans.semiBold,
    fontSize: 16,
    lineHeight: 20,
    color: THEME.COLORS.blackSub,
  },
});